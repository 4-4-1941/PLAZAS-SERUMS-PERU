(()=>{"use strict";
const $=id=>document.getElementById(id),e={conv:$("f-convocatoria"),mod:$("f-modalidad"),tipo:$("f-tipo-institucion"),pro:$("f-profesion"),dep:$("f-departamento"),prov:$("f-provincia"),dist:$("f-distrito"),btn:$("btn-buscar"),res:$("resultados"),estado:$("estado-datos"),cmp:$("comparador"),cmpCount:$("comparador-count"),cmpSec:$("comparacion"),dock:$("compare-dock"),dockText:$("dock-text"),verCmp:$("btn-ver-comparacion"),limpiar:$("btn-limpiar-comparacion"),priorizar:$("btn-priorizar"),pri:$("lista-prioridades"),priCount:$("prioridad-count"),aviso:$("aviso-filtros"),cmpView:$("compare-view"),cmpMovil:$("comparador-movil"),cerrarCmp:$("btn-cerrar-comparacion"),guardarAlt:$("btn-guardar-alternativas"),limpiarMovil:$("btn-limpiar-movil")};
if(Object.values(e).some(x=>!x)){console.error("PLAZAS SERUMS: controles faltantes");return}
const cache=new Map(),seleccion=new Map(),prioridades=[],esc=v=>String(v??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[c])),clave=v=>String(v??"").replace(/Ñ/g,"ZZZENYEZZZ").replace(/ñ/g,"zzzenyezzz").normalize("NFD").replace(/[\u0300-\u036f]/g,"").replace(/ZZZENYEZZZ/g,"Ñ").replace(/zzzenyezzz/g,"ñ").toUpperCase().replace(/[^A-Z0-9Ñ]+/g," ").trim(),acentos=v=>(String(v).match(/[ÁÉÍÓÚÜáéíóúü]/g)||[]).length,mejor=(a,b)=>acentos(b)>acentos(a)?b:a,unicos=vs=>{const m=new Map();vs.filter(Boolean).forEach(v=>{const k=clave(v);m.set(k,m.has(k)?mejor(m.get(k),v):v)});return[...m.values()].sort((a,b)=>a.localeCompare(b,"es",{sensitivity:"base"}))},root=()=>`data/${e.conv.value}/`,file=()=>e.mod.value==="remuneradas"?"remuneradas":e.mod.value==="equivalentes"?"equivalentes":"",opts=(el,vs,ph)=>{el.innerHTML=`<option value="">${esc(ph)}</option>`+vs.map(v=>`<option value="${esc(v)}">${esc(v)}</option>`).join("");el.disabled=!vs.length};
async function getJSON(path){const k=root()+path;if(cache.has(k))return cache.get(k);const r=await fetch(k,{cache:"no-cache"});if(!r.ok)throw Error(`${k}: HTTP ${r.status}`);const d=await r.json();cache.set(k,d);return d}
async function catalogo(){const m=file();if(!m)return null;e.estado.textContent="Cargando filtros…";const c=await getJSON(`catalogo-${m}.json`);e.estado.textContent=`${c.registros.toLocaleString("es-PE")} registros disponibles.`;return c}
const merge=os=>{const out={};os.forEach(o=>Object.entries(o||{}).forEach(([k,v])=>{const old=Object.keys(out).find(x=>clave(x)===clave(k));if(old&&Array.isArray(v))out[old]=unicos([...out[old],...v]);else if(old&&v&&typeof v==="object")out[old]=merge([out[old],v]);else out[k]=v}));return out};
function ramas(c){const p=e.pro.value;return p?merge(Object.entries(c.filtros).filter(([k])=>clave(k)===clave(p)).map(([,v])=>v)):merge(Object.values(c.filtros||{}))}
const deps=c=>unicos(Object.keys(ramas(c))),provs=c=>{const r=ramas(c),d=e.dep.value;return d?unicos(Object.entries(r).filter(([k])=>clave(k)===clave(d)).flatMap(([,v])=>Object.keys(v))):[]},dists=c=>{const r=ramas(c),d=e.dep.value,p=e.prov.value;if(!d||!p)return[];return unicos(Object.entries(r).filter(([k])=>clave(k)===clave(d)).flatMap(([,v])=>Object.entries(v).filter(([k])=>clave(k)===clave(p)).flatMap(([,x])=>x)))};
const baseTipo=(data)=>data.filter(r=>!e.tipo.value||tipoInstitucion(r)===e.tipo.value);
const porProfesion=(data)=>baseTipo(data).filter(r=>igual((r.oficial_minsa||{}).profesion,e.pro.value));
const porDepartamento=(data)=>porProfesion(data).filter(r=>igual((r.normalizado||{}).departamento||(r.oficial_minsa||{}).departamento,e.dep.value));
const porProvincia=(data)=>porDepartamento(data).filter(r=>igual((r.oficial_minsa||{}).provincia,e.prov.value));
async function datosActuales(){const m=file();return m?getJSON(`${m}.json`):[]}
async function reconstruirDesdeDatos(nivel="tipo"){
  if(!file())return;
  const data=await datosActuales(),base=baseTipo(data);e.aviso.hidden=true;e.aviso.innerHTML="";
  if(!base.length){
    opts(e.pro,[],"Sin plazas disponibles");opts(e.dep,[],"Sin plazas disponibles");opts(e.prov,[],"Sin plazas disponibles");opts(e.dist,[],"Sin plazas disponibles");
    e.btn.disabled=true;
    if(e.tipo.value==="educacion"){
      const otra=e.mod.value==="remuneradas"?"equivalentes":"remuneradas",otraData=await getJSON(`${otra}.json`),hayOtra=otraData.some(r=>tipoInstitucion(r)==="educacion");
      const actual=e.mod.value==="remuneradas"?"remuneradas":"equivalentes",alt=otra==="equivalentes"?"Equivalentes":"Remuneradas";
      e.estado.textContent=`No hay plazas ${actual} en instituciones educativas en SERUMS ${e.conv.value}.`;
      e.aviso.hidden=false;e.aviso.innerHTML=`<strong>No hay plazas ${esc(actual)} en instituciones educativas para ${esc(e.conv.value)}.</strong>${hayOtra?` Sí existen en la otra modalidad. <button id="btn-ver-otra">Ver ${alt}</button>`:""}`;
      if(hayOtra){const b=$("btn-ver-otra");if(b)b.onclick=()=>{e.mod.value=otra;safe(resetModalidad)}}
    }else{e.estado.textContent="No hay plazas disponibles para esta selección.";e.aviso.hidden=false;e.aviso.textContent="No existen plazas con esta combinación. Cambia uno de los filtros anteriores."}
    return;
  }
  if(nivel==="tipo"){
    opts(e.pro,unicos(base.map(r=>(r.oficial_minsa||{}).profesion)),"Todas las profesiones");
    opts(e.dep,unicos(base.map(r=>(r.normalizado||{}).departamento||(r.oficial_minsa||{}).departamento)),"Todos los departamentos");
    opts(e.prov,[],"Selecciona departamento");opts(e.dist,[],"Selecciona provincia");
  }
  if(nivel==="pro"){
    const rows=porProfesion(data);
    opts(e.dep,unicos(rows.map(r=>(r.normalizado||{}).departamento||(r.oficial_minsa||{}).departamento)),"Todos los departamentos");
    opts(e.prov,[],"Selecciona departamento");opts(e.dist,[],"Selecciona provincia");
  }
  if(nivel==="dep"){
    const rows=porDepartamento(data);
    opts(e.prov,unicos(rows.map(r=>(r.oficial_minsa||{}).provincia)),"Todas las provincias");
    opts(e.dist,[],"Selecciona provincia");
  }
  if(nivel==="prov"){
    const rows=porProvincia(data);
    opts(e.dist,unicos(rows.map(r=>(r.oficial_minsa||{}).distrito)),"Todos los distritos");
  }
  e.btn.disabled=false;
  e.estado.textContent=`${base.length.toLocaleString("es-PE")} registros disponibles para el tipo de institución seleccionado.`;
}
async function resetModalidad(){
  if(!file()){opts(e.pro,[],"Selecciona modalidad");opts(e.dep,[],"Selecciona modalidad");opts(e.prov,[],"Selecciona departamento");opts(e.dist,[],"Selecciona provincia");e.btn.disabled=true;return}
  e.estado.textContent="Cargando filtros disponibles…";await reconstruirDesdeDatos("tipo");
}
const tipoInstitucion=r=>clave((r.oficial_minsa||{}).institucion)==="MINEDU"?"educacion":"salud",igual=(a,b)=>!b||clave(a)===clave(b),match=r=>{const x=r.oficial_minsa||{},n=r.normalizado||{};return (!e.tipo.value||tipoInstitucion(r)===e.tipo.value)&&igual(x.profesion,e.pro.value)&&igual(n.departamento||x.departamento,e.dep.value)&&igual(x.provincia,e.prov.value)&&igual(x.distrito,e.dist.value)},key=r=>`${r.convocatoria}|${r.modalidad}|${r.id}`,mod=m=>m==="remuneradas"?"Remunerada":"Equivalente";
const campos=[["Convocatoria",r=>r.convocatoria],["Modalidad",r=>mod(r.modalidad)],["Tipo de institución",r=>tipoInstitucion(r)==="educacion"?"Institución educativa":"Institución de salud"],["Establecimiento",r=>r.oficial_minsa.establecimiento],["Profesión",r=>r.oficial_minsa.profesion],["Departamento",r=>r.oficial_minsa.departamento],["Provincia",r=>r.oficial_minsa.provincia],["Distrito",r=>r.oficial_minsa.distrito],["Grado dificultad",r=>r.oficial_minsa.grado_dificultad],["N° plazas",r=>r.oficial_minsa.numero_plazas],["Categoría",r=>r.oficial_minsa.categoria],["Institución",r=>r.oficial_minsa.institucion],["Presupuesto",r=>r.oficial_minsa.presupuesto],["ZAF",r=>r.oficial_minsa.zaf],["ZE",r=>r.oficial_minsa.ze],["RENIPRESS",r=>r.oficial_minsa.codigo_renipress]];
function sync(){document.querySelectorAll("[data-compare]").forEach(b=>{const on=seleccion.has(b.dataset.compare);b.textContent=on?"Quitar de comparación":"Comparar";b.classList.toggle("selected",on)})}
function renderCmp(){const rows=[...seleccion.values()],n=rows.length;e.cmpCount.textContent=`${n}/3 seleccionadas`;e.dockText.textContent=`${n} plaza${n===1?"":"s"} seleccionada${n===1?"":"s"}`;e.dock.classList.toggle("visible",n>0);e.limpiar.disabled=!n;e.priorizar.disabled=!n;if(!n){e.cmp.innerHTML='<div class="placeholder">Selecciona una plaza con “Comparar”. Aquí aparecerá automáticamente.</div>';e.cmpMovil.innerHTML="";return}const html=`<div class="compare-cards">${rows.map((r,i)=>`<article class="compare-card"><h3>Plaza ${i+1}<button class="remove-compare" data-remove="${esc(key(r))}">×</button></h3>${campos.map(([n,f])=>`<div class="compare-row"><b>${esc(n)}</b><span>${esc(f(r))}</span></div>`).join("")}</article>`).join("")}</div>`;e.cmp.innerHTML=html;e.cmpMovil.innerHTML=html}
function toggle(k,r){if(seleccion.has(k))seleccion.delete(k);else{if(seleccion.size>=3){e.estado.textContent="Máximo 3 plazas para comparar.";e.cmpSec.scrollIntoView({behavior:"smooth"});return}seleccion.set(k,r)}renderCmp();sync()}
function renderPri(){e.priCount.textContent=`${prioridades.length} alternativa${prioridades.length===1?"":"s"}`;if(!prioridades.length){e.pri.innerHTML='<div class="placeholder"><strong>Aún no has guardado alternativas.</strong><br>Selecciona plazas con “Comparar”, pulsa “Ver comparación” y luego “Guardar en mis alternativas”. No tienes que escribir nada aquí.</div>';return}e.pri.innerHTML=`<div class="priority-list">${prioridades.map((r,i)=>{const x=r.oficial_minsa;return`<article class="priority-item"><div class="priority-num">${i+1}</div><div><strong>${esc(x.establecimiento)}</strong><div>${esc(x.distrito)} · ${esc(x.provincia)} · ${esc(x.departamento)}</div><div class="note">${esc(r.convocatoria)} · ${esc(mod(r.modalidad))} · ${tipoInstitucion(r)==="educacion"?"Institución educativa":"Institución de salud"} · ${esc(x.profesion)} · ${esc(x.grado_dificultad)} · RENIPRESS ${esc(x.codigo_renipress)}</div></div><div class="priority-actions"><button data-up="${i}">↑</button><button data-down="${i}">↓</button><button data-del="${i}">×</button></div></article>`}).join("")}</div>`}
function agregar(){[...seleccion.values()].forEach(r=>{if(!prioridades.some(x=>key(x)===key(r)))prioridades.push(r)});renderPri();seleccion.clear();renderCmp();sync();cerrarComparacion();e.pri.scrollIntoView({behavior:"smooth"})}
function abrirComparacion(){if(!seleccion.size)return;e.cmpView.hidden=false;e.cmpView.setAttribute("aria-hidden","false");document.body.style.overflow="hidden";renderCmp()}
function cerrarComparacion(){e.cmpView.hidden=true;e.cmpView.setAttribute("aria-hidden","true");document.body.style.overflow=""}
async function buscar(){const m=file();e.estado.textContent="Cargando plazas oficiales…";const data=await getJSON(`${m}.json`),rows=data.filter(match),plazas=rows.reduce((s,r)=>s+Number(r.oficial_minsa?.numero_plazas||0),0),MAX=200;e.estado.textContent=`${rows.length.toLocaleString("es-PE")} registros encontrados.`;if(!rows.length){e.res.innerHTML='<div class="placeholder">No se encontraron plazas.</div>';return}e.res.className="";e.res.innerHTML=`<div class="result-summary"><strong>${plazas.toLocaleString("es-PE")} plazas</strong> · ${rows.length.toLocaleString("es-PE")} registros · ${esc(e.conv.value)}${rows.length>MAX?` · primeros ${MAX}`:""}</div><div class="result-list">${rows.slice(0,MAX).map(r=>{const x=r.oficial_minsa||{},n=r.normalizado||{},k=`${e.conv.value}|${m}|${r.id}`;return`<article class="plaza-card"><div class="plaza-head"><strong>${esc(x.establecimiento)}</strong><span>${esc(x.grado_dificultad)}</span></div><div>${esc(x.distrito)} · ${esc(x.provincia)} · ${esc(n.departamento||x.departamento)}</div><div><b>${esc(x.profesion)}</b> · ${esc(x.numero_plazas)} plaza(s) · <b>${esc(mod(m))}</b></div><div>Tipo: <b>${tipoInstitucion(r)==="educacion"?"Institución educativa":"Institución de salud"}</b></div><div>RENIPRESS: ${esc(x.codigo_renipress)} · Categoría: ${esc(x.categoria)}</div><div>Institución: ${esc(x.institucion)} · Presupuesto: ${esc(x.presupuesto)}</div><div>ZAF: ${esc(x.zaf)} · ZE: ${esc(x.ze)}</div><div class="card-actions"><button class="compare-btn${seleccion.has(k)?" selected":""}" data-compare="${esc(k)}">${seleccion.has(k)?"Quitar de comparación":"Comparar"}</button></div></article>`}).join("")}</div>`;e.res.querySelectorAll("[data-compare]").forEach(b=>b.onclick=()=>{const r=rows.find(x=>`${e.conv.value}|${m}|${x.id}`===b.dataset.compare);if(r)toggle(b.dataset.compare,{...r,convocatoria:e.conv.value,modalidad:m})})}
async function safe(fn){try{await fn()}catch(err){console.error(err);e.estado.textContent="Error al cargar datos."}}
function resetConv(){e.mod.value="";opts(e.pro,[],"Selecciona modalidad");opts(e.dep,[],"Selecciona modalidad");opts(e.prov,[],"Selecciona departamento");opts(e.dist,[],"Selecciona provincia");e.btn.disabled=true;e.aviso.hidden=true;e.aviso.innerHTML="";e.estado.textContent=`Convocatoria ${e.conv.value}. Selecciona modalidad.`}
const quitarComparacion=ev=>{const b=ev.target.closest("[data-remove]");if(b){seleccion.delete(b.dataset.remove);renderCmp();sync();if(!seleccion.size)cerrarComparacion()}};e.cmp.onclick=quitarComparacion;e.cmpMovil.onclick=quitarComparacion;
e.pri.onclick=ev=>{const u=ev.target.closest("[data-up]"),d=ev.target.closest("[data-down]"),x=ev.target.closest("[data-del]");if(u){let i=+u.dataset.up;if(i>0)[prioridades[i-1],prioridades[i]]=[prioridades[i],prioridades[i-1]]}if(d){let i=+d.dataset.down;if(i<prioridades.length-1)[prioridades[i+1],prioridades[i]]=[prioridades[i],prioridades[i+1]]}if(x)prioridades.splice(+x.dataset.del,1);renderPri()};
e.verCmp.onclick=abrirComparacion;e.cerrarCmp.onclick=cerrarComparacion;e.guardarAlt.onclick=agregar;e.limpiarMovil.onclick=()=>{seleccion.clear();renderCmp();sync();cerrarComparacion()};e.limpiar.onclick=()=>{seleccion.clear();renderCmp();sync()};e.priorizar.onclick=agregar;e.conv.onchange=resetConv;e.mod.onchange=()=>safe(resetModalidad);e.tipo.onchange=()=>safe(()=>reconstruirDesdeDatos("tipo"));e.pro.onchange=()=>safe(()=>reconstruirDesdeDatos("pro"));e.dep.onchange=()=>safe(()=>reconstruirDesdeDatos("dep"));e.prov.onchange=()=>safe(()=>reconstruirDesdeDatos("prov"));e.btn.onclick=()=>safe(buscar);renderCmp();renderPri();e.estado.textContent="Selecciona convocatoria y modalidad para cargar filtros.";
})();