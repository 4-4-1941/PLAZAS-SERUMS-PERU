(()=>{"use strict";
const $=id=>document.getElementById(id),e={conv:$("f-convocatoria"),mod:$("f-modalidad"),tipo:$("f-tipo-institucion"),pro:$("f-profesion"),dep:$("f-departamento"),prov:$("f-provincia"),dist:$("f-distrito"),btn:$("btn-buscar"),res:$("resultados"),estado:$("estado-datos"),cmp:$("comparador"),cmpCount:$("comparador-count"),cmpSec:$("comparacion"),dock:$("compare-dock"),dockText:$("dock-text"),verCmp:$("btn-ver-comparacion"),limpiar:$("btn-limpiar-comparacion"),priorizar:$("btn-priorizar"),pri:$("lista-prioridades"),priCount:$("prioridad-count"),aviso:$("aviso-filtros"),cmpView:$("compare-view"),cmpMovil:$("comparador-movil"),cerrarCmp:$("btn-cerrar-comparacion"),guardarAlt:$("btn-guardar-alternativas")};
if(Object.values(e).some(x=>!x)){console.error("PLAZAS SERUMS: controles faltantes");return}
const cache=new Map(),seleccion=new Map(),prioridades=[],limiteComparacion=()=>matchMedia("(max-width:850px)").matches?3:5,esc=v=>String(v??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[c])),clave=v=>String(v??"").replace(/Ñ/g,"ZZZENYEZZZ").replace(/ñ/g,"zzzenyezzz").normalize("NFD").replace(/[\u0300-\u036f]/g,"").replace(/ZZZENYEZZZ/g,"Ñ").replace(/zzzenyezzz/g,"ñ").toUpperCase().replace(/[^A-Z0-9Ñ]+/g," ").trim(),acentos=v=>(String(v).match(/[ÁÉÍÓÚÜáéíóúü]/g)||[]).length,mejor=(a,b)=>acentos(b)>acentos(a)?b:a,unicos=vs=>{const m=new Map();vs.filter(Boolean).forEach(v=>{const k=clave(v);m.set(k,m.has(k)?mejor(m.get(k),v):v)});return[...m.values()].sort((a,b)=>a.localeCompare(b,"es",{sensitivity:"base"}))},root=()=>`data/${e.conv.value}/`,file=()=>e.mod.value==="remuneradas"?"remuneradas":e.mod.value==="equivalentes"?"equivalentes":"",opts=(el,vs,ph)=>{el.innerHTML=`<option value="">${esc(ph)}</option>`+vs.map(v=>`<option value="${esc(v)}">${esc(v)}</option>`).join("");el.disabled=!vs.length};
async function getJSON(path){const k=root()+path;if(cache.has(k))return cache.get(k);const r=await fetch(k,{cache:"no-cache"});if(!r.ok)throw Error(`${k}: HTTP ${r.status}`);const d=await r.json();if(Array.isArray(d))d.forEach(r=>{const x=r&&r.oficial_minsa;if(x&&clave(x.profesion)==="CLINICO ENFERMERIA")x.profesion="ENFERMERIA"});cache.set(k,d);return d}
const tipoInstitucion=r=>clave((r.oficial_minsa||{}).institucion)==="MINEDU"?"educacion":"salud",igual=(a,b)=>!b||clave(a)===clave(b),baseTipo=data=>data.filter(r=>!e.tipo.value||tipoInstitucion(r)===e.tipo.value),porProfesion=data=>baseTipo(data).filter(r=>igual((r.oficial_minsa||{}).profesion,e.pro.value)),porDepartamento=data=>porProfesion(data).filter(r=>igual((r.normalizado||{}).departamento||(r.oficial_minsa||{}).departamento,e.dep.value)),porProvincia=data=>porDepartamento(data).filter(r=>igual((r.oficial_minsa||{}).provincia,e.prov.value));
async function datosActuales(){const m=file();return m?getJSON(`${m}.json`):[]}
async function reconstruirDesdeDatos(nivel="tipo"){if(!file())return;const data=await datosActuales(),base=baseTipo(data);e.aviso.hidden=true;e.aviso.innerHTML="";if(!base.length){opts(e.pro,[],"Sin plazas disponibles");opts(e.dep,[],"Sin plazas disponibles");opts(e.prov,[],"Sin plazas disponibles");opts(e.dist,[],"Sin plazas disponibles");e.btn.disabled=true;if(e.tipo.value==="educacion"){const otra=e.mod.value==="remuneradas"?"equivalentes":"remuneradas",otraData=await getJSON(`${otra}.json`),hayOtra=otraData.some(r=>tipoInstitucion(r)==="educacion"),actual=e.mod.value==="remuneradas"?"remuneradas":"equivalentes",alt=otra==="equivalentes"?"Equivalentes":"Remuneradas";e.estado.textContent=`No hay plazas ${actual} en instituciones educativas en SERUMS ${e.conv.value}.`;e.aviso.hidden=false;e.aviso.innerHTML=`<strong>No hay plazas ${esc(actual)} en instituciones educativas para ${esc(e.conv.value)}.</strong>${hayOtra?` Sí existen en la otra modalidad. <button id="btn-ver-otra">Ver ${alt}</button>`:""}`;if(hayOtra){const b=$("btn-ver-otra");if(b)b.onclick=()=>{e.mod.value=otra;safe(resetModalidad)}}}else{e.estado.textContent="No hay plazas disponibles para esta selección.";e.aviso.hidden=false;e.aviso.textContent="No existen plazas con esta combinación. Cambia uno de los filtros anteriores."}return}
if(nivel==="tipo"){opts(e.pro,unicos(base.map(r=>(r.oficial_minsa||{}).profesion)),"Todas las profesiones");opts(e.dep,unicos(base.map(r=>(r.normalizado||{}).departamento||(r.oficial_minsa||{}).departamento)),"Todos los departamentos");opts(e.prov,[],"Selecciona departamento");opts(e.dist,[],"Selecciona provincia")}
if(nivel==="pro"){const rows=porProfesion(data);opts(e.dep,unicos(rows.map(r=>(r.normalizado||{}).departamento||(r.oficial_minsa||{}).departamento)),"Todos los departamentos");opts(e.prov,[],"Selecciona departamento");opts(e.dist,[],"Selecciona provincia")}
if(nivel==="dep"){const rows=porDepartamento(data);opts(e.prov,unicos(rows.map(r=>(r.oficial_minsa||{}).provincia)),"Todas las provincias");opts(e.dist,[],"Selecciona provincia")}
if(nivel==="prov"){const rows=porProvincia(data);opts(e.dist,unicos(rows.map(r=>(r.oficial_minsa||{}).distrito)),"Todos los distritos")}e.btn.disabled=false;e.estado.textContent=`${base.length.toLocaleString("es-PE")} registros disponibles para el tipo de institución seleccionado.`}
async function resetModalidad(){if(!file()){opts(e.pro,[],"Selecciona modalidad");opts(e.dep,[],"Selecciona modalidad");opts(e.prov,[],"Selecciona departamento");opts(e.dist,[],"Selecciona provincia");e.btn.disabled=true;return}e.estado.textContent="Cargando filtros disponibles…";await reconstruirDesdeDatos("tipo")}
const match=r=>{const x=r.oficial_minsa||{},n=r.normalizado||{};return(!e.tipo.value||tipoInstitucion(r)===e.tipo.value)&&igual(x.profesion,e.pro.value)&&igual(n.departamento||x.departamento,e.dep.value)&&igual(x.provincia,e.prov.value)&&igual(x.distrito,e.dist.value)},key=r=>`${r.convocatoria}|${r.modalidad}|${r.id}`,mod=m=>m==="remuneradas"?"Remunerada":"Equivalente",campos=[["Convocatoria",r=>r.convocatoria],["Modalidad",r=>mod(r.modalidad)],["Tipo",r=>tipoInstitucion(r)==="educacion"?"Institución educativa":"Institución de salud"],["Establecimiento",r=>r.oficial_minsa.establecimiento],["Profesión",r=>r.oficial_minsa.profesion],["Departamento",r=>r.oficial_minsa.departamento],["Provincia",r=>r.oficial_minsa.provincia],["Distrito",r=>r.oficial_minsa.distrito],["Grado dificultad",r=>r.oficial_minsa.grado_dificultad],["N° plazas",r=>r.oficial_minsa.numero_plazas],["Categoría",r=>r.oficial_minsa.categoria],["Institución",r=>r.oficial_minsa.institucion],["Presupuesto",r=>r.oficial_minsa.presupuesto],["ZAF",r=>r.oficial_minsa.zaf],["ZE",r=>r.oficial_minsa.ze],["RENIPRESS",r=>r.oficial_minsa.codigo_renipress]],estaGuardada=k=>prioridades.some(r=>key(r)===k);
function sync(){document.querySelectorAll("[data-compare]").forEach(b=>{const on=seleccion.has(b.dataset.compare);b.textContent=on?"Quitar":"Comparar";b.classList.toggle("selected",on)});document.querySelectorAll("[data-save]").forEach(b=>{const on=estaGuardada(b.dataset.save);b.textContent=on?"Guardada ✓":"Guardar";b.disabled=on})}
function renderCmp(){const rows=[...seleccion.values()],n=rows.length;e.cmpCount.textContent=`${n}/${limiteComparacion()} seleccionadas`;e.dockText.textContent=n<2?(n===1?"Selecciona otra plaza para comparar":"0 plazas seleccionadas"):`${n} plazas listas para comparar`;e.dock.classList.toggle("visible",n>0);e.verCmp.disabled=n<2;e.limpiar.disabled=!n;e.priorizar.disabled=!n;if(!n){e.cmp.innerHTML='<div class="empty-state">Selecciona dos o más plazas para compararlas.</div>';e.cmpMovil.innerHTML="";return}const html=`<div class="compare-cards">${rows.map((r,i)=>`<article class="compare-card"><h3>Plaza ${i+1}<button class="remove-compare" data-remove="${esc(key(r))}">×</button></h3>${campos.map(([n,f])=>`<div class="compare-row"><b>${esc(n)}</b><span>${esc(f(r))}</span></div>`).join("")}</article>`).join("")}</div>`;e.cmp.innerHTML=n>=2?html:'<div class="empty-state">Selecciona otra plaza para comparar, o usa “Guardar” directamente en el resultado.</div>';e.cmpMovil.innerHTML=n>=2?html:""}
function toggle(k,r){if(seleccion.has(k))seleccion.delete(k);else{if(seleccion.size>=limiteComparacion()){e.estado.textContent=`Máximo ${limiteComparacion()} plazas para comparar en este dispositivo.`;return}seleccion.set(k,r)}renderCmp();sync()}
function renderPri(){e.priCount.textContent=`${prioridades.length} alternativa${prioridades.length===1?"":"s"}`;if(!prioridades.length){e.pri.innerHTML='<div class="empty-state">Guarda las plazas que te interesen. Aquí podrás ordenarlas según tu prioridad.</div>';return}e.pri.innerHTML=`<div class="priority-list">${prioridades.map((r,i)=>{const x=r.oficial_minsa;return`<article class="priority-item"><div class="priority-num">${i+1}</div><div><strong>${esc(x.establecimiento)}</strong><div>${esc(x.distrito)} · ${esc(x.provincia)} · ${esc(x.departamento)}</div><div class="note">${esc(r.convocatoria)} · ${esc(mod(r.modalidad))} · ${tipoInstitucion(r)==="educacion"?"Institución educativa":"Institución de salud"} · ${esc(x.profesion)} · ${esc(x.grado_dificultad)} · RENIPRESS ${esc(x.codigo_renipress)}</div></div><div class="priority-actions"><button data-up="${i}">↑</button><button data-down="${i}">↓</button><button data-del="${i}">×</button></div></article>`}).join("")}</div>`}
function emitirAlternativas(){dispatchEvent(new CustomEvent("serums:alternativas",{detail:{alternativas:[...prioridades]}}))}
function guardarUna(r,scroll=false){if(!estaGuardada(key(r)))prioridades.push(r);renderPri();emitirAlternativas();sync();if(scroll)e.pri.scrollIntoView({behavior:"smooth",block:"start"})}
function agregar(){[...seleccion.values()].forEach(r=>{if(!estaGuardada(key(r)))prioridades.push(r)});seleccion.clear();renderPri();emitirAlternativas();renderCmp();sync();cerrarComparacion();e.pri.scrollIntoView({behavior:"smooth",block:"start"})}
function abrirComparacion(){if(seleccion.size<2){e.estado.textContent="Selecciona al menos dos plazas para comparar.";return}e.cmpView.hidden=false;e.cmpView.setAttribute("aria-hidden","false");document.body.style.overflow="hidden";renderCmp()}
function cerrarComparacion(){e.cmpView.hidden=true;e.cmpView.setAttribute("aria-hidden","true");document.body.style.overflow=""}
async function buscar(){seleccion.clear();renderCmp();sync();const m=file();e.estado.textContent="Cargando plazas oficiales…";const data=await getJSON(`${m}.json`),rows=data.filter(match),plazas=rows.reduce((s,r)=>s+Number(r.oficial_minsa?.numero_plazas||0),0),MAX=200;e.estado.textContent=`${rows.length.toLocaleString("es-PE")} registros encontrados.`;if(!rows.length){e.res.innerHTML='<div class="empty-state">No se encontraron plazas.</div>';return}e.res.className="";e.res.innerHTML=`<div class="result-summary"><strong>${plazas.toLocaleString("es-PE")} plazas</strong> · ${rows.length.toLocaleString("es-PE")} registros · ${esc(e.conv.value)}${rows.length>MAX?` · primeros ${MAX}`:""}</div><div class="result-list">${rows.slice(0,MAX).map(r=>{const x=r.oficial_minsa||{},n=r.normalizado||{},k=`${e.conv.value}|${m}|${r.id}`,saved=estaGuardada(k);return`<article class="plaza-card"><div class="plaza-head"><strong>${esc(x.establecimiento)}</strong><span>${esc(x.grado_dificultad)}</span></div><div>${esc(x.distrito)} · ${esc(x.provincia)} · ${esc(n.departamento||x.departamento)}</div><div><b>${esc(x.profesion)}</b> · ${esc(x.numero_plazas)} plaza(s) · <b>${esc(mod(m))}</b></div><div>Tipo: <b>${tipoInstitucion(r)==="educacion"?"Institución educativa":"Institución de salud"}</b></div><div>RENIPRESS: ${esc(x.codigo_renipress)} · Categoría: ${esc(x.categoria)}</div><div>Institución: ${esc(x.institucion)} · Presupuesto: ${esc(x.presupuesto)}</div><div>ZAF: ${esc(x.zaf)} · ZE: ${esc(x.ze)}</div><div class="card-actions"><button class="save-btn" data-save="${esc(k)}"${saved?" disabled":""}>${saved?"Guardada ✓":"Guardar"}</button><button class="compare-btn${seleccion.has(k)?" selected":""}" data-compare="${esc(k)}">${seleccion.has(k)?"Quitar":"Comparar"}</button></div></article>`}).join("")}</div>`;const getRow=k=>{const r=rows.find(x=>`${e.conv.value}|${m}|${x.id}`===k);return r?{...r,convocatoria:e.conv.value,modalidad:m}:null};e.res.querySelectorAll("[data-save]").forEach(b=>b.onclick=()=>{const r=getRow(b.dataset.save);if(r)guardarUna(r,true)});e.res.querySelectorAll("[data-compare]").forEach(b=>b.onclick=()=>{const r=getRow(b.dataset.compare);if(r)toggle(b.dataset.compare,r)})}
async function safe(fn){try{await fn()}catch(err){console.error(err);e.estado.textContent="Error al cargar datos."}}
function resetConv(){e.mod.value="";opts(e.pro,[],"Selecciona modalidad");opts(e.dep,[],"Selecciona modalidad");opts(e.prov,[],"Selecciona departamento");opts(e.dist,[],"Selecciona provincia");e.btn.disabled=true;e.aviso.hidden=true;e.aviso.innerHTML="";e.estado.textContent=`Convocatoria ${e.conv.value}. Selecciona modalidad.`}
const quitarComparacion=ev=>{const b=ev.target.closest("[data-remove]");if(b){seleccion.delete(b.dataset.remove);renderCmp();sync();if(seleccion.size<2)cerrarComparacion()}};e.cmp.onclick=quitarComparacion;e.cmpMovil.onclick=quitarComparacion;
e.pri.onclick=ev=>{const u=ev.target.closest("[data-up]"),d=ev.target.closest("[data-down]"),x=ev.target.closest("[data-del]");if(u){let i=+u.dataset.up;if(i>0)[prioridades[i-1],prioridades[i]]=[prioridades[i],prioridades[i-1]]}if(d){let i=+d.dataset.down;if(i<prioridades.length-1)[prioridades[i+1],prioridades[i]]=[prioridades[i],prioridades[i+1]]}if(x)prioridades.splice(+x.dataset.del,1);renderPri();emitirAlternativas();sync()};
e.verCmp.onclick=abrirComparacion;e.cerrarCmp.onclick=cerrarComparacion;e.guardarAlt.onclick=agregar;e.limpiar.onclick=()=>{seleccion.clear();renderCmp();sync()};e.priorizar.onclick=agregar;e.conv.onchange=resetConv;e.mod.onchange=()=>safe(resetModalidad);e.tipo.onchange=()=>safe(()=>reconstruirDesdeDatos("tipo"));e.pro.onchange=()=>safe(()=>reconstruirDesdeDatos("pro"));e.dep.onchange=()=>safe(()=>reconstruirDesdeDatos("dep"));e.prov.onchange=()=>safe(()=>reconstruirDesdeDatos("prov"));e.btn.onclick=()=>safe(buscar);renderCmp();renderPri();e.estado.textContent="Selecciona convocatoria y modalidad para cargar filtros.";
})();